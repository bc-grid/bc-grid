import { expect, test } from "bun:test"

// Placeholder smoke test — keeps `bun test` green until real tests land.
// Real package-level tests live in each package's `tests/` directory.
test("scaffold smoke", () => {
  expect(true).toBe(true)
})
