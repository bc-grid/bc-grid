{
  "name": "@bc-grid/app-animation-benchmarks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5175",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4175",
    "type-check": "tsc -b"
  },
  "dependencies": {
    "@bc-grid/animations": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
