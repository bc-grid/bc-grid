{
  "$schema": "https://biomejs.dev/schemas/1.9.4/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "ignore": [
      "**/dist/**",
      "**/node_modules/**",
      "**/.next/**",
      "**/.astro/**",
      "**/.turbo/**",
      "**/*.tsbuildinfo"
    ]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true
    }
  },
  "overrides": [
    {
      "include": [
        "packages/react/src/grid.tsx",
        "packages/react/src/bodyCells.tsx",
        "packages/react/src/headerCells.tsx"
      ],
      "linter": {
        "rules": {
          "a11y": {
            "noNoninteractiveTabindex": "off",
            "useFocusableInteractive": "off",
            "useKeyWithClickEvents": "off",
            "useSemanticElements": "off"
          }
        }
      }
    },
    {
      "include": ["packages/react/src/types.ts"],
      "linter": {
        "rules": {
          "suspicious": {
            "noConfusingVoidType": "off"
          }
        }
      }
    },
    {
      "include": ["packages/*/tests/**/*.ts", "packages/*/tests/**/*.tsx"],
      "linter": {
        "rules": {
          "nursery": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "../../editors/src/autocomplete": "Tests must import built-in editors from @bc-grid/editors so the public package surface is covered.",
                  "../../editors/src/checkbox": "Tests must import built-in editors from @bc-grid/editors so the public package surface is covered.",
                  "../../editors/src/date": "Tests must import built-in editors from @bc-grid/editors so the public package surface is covered.",
                  "../../editors/src/datetime": "Tests must import built-in editors from @bc-grid/editors so the public package surface is covered.",
                  "../../editors/src/multiSelect": "Tests must import built-in editors from @bc-grid/editors so the public package surface is covered.",
                  "../../editors/src/number": "Tests must import built-in editors from @bc-grid/editors so the public package surface is covered.",
                  "../../editors/src/select": "Tests must import built-in editors from @bc-grid/editors so the public package surface is covered.",
                  "../../editors/src/text": "Tests must import built-in editors from @bc-grid/editors so the public package surface is covered.",
                  "../../editors/src/time": "Tests must import built-in editors from @bc-grid/editors so the public package surface is covered."
                }
              }
            }
          }
        }
      }
    }
  ],
  "javascript": {
    "formatter": {
      "quoteStyle": "double",
      "semicolons": "asNeeded",
      "trailingCommas": "all"
    }
  }
}
